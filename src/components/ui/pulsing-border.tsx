/* 21st.dev template: shader-driven pulsing border (@paper-design). Used as
   the "background glow" halo behind the two money CTAs (hero quiz button,
   CTA band) — space-station energy, brand embers only, low speed so it
   reads ambient rather than busy. */
export {
  PulsingBorder,
  pulsingBorderPresets,
} from "@paper-design/shaders-react";
export type { PulsingBorderProps } from "@paper-design/shaders-react";

import { PulsingBorder } from "@paper-design/shaders-react";

export default PulsingBorder;
